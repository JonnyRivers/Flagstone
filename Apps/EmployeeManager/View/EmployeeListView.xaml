<UserControl x:Class="EmployeeManager.View.EmployeeListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:EmployeeManager.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="400"
             d:DataContext="{d:DesignInstance vm:EmployeeListDesignTimeViewModel, IsDesignTimeCreatable=True}">
    <StackPanel>
        <ListView ItemsSource="{Binding AllEmployees}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="First Name" Width="70" DisplayMemberBinding="{Binding FirstName}" />
                    <GridViewColumn Header="Last Name" Width="70" DisplayMemberBinding="{Binding LastName}" />
                    <GridViewColumn Header="Date of Birth" Width="100" DisplayMemberBinding="{Binding DateOfBirth, StringFormat=d}" />
                    <GridViewColumn Header="Department" Width="70" DisplayMemberBinding="{Binding Department.Name}" />
                </GridView>
            </ListView.View>
        </ListView>
    </StackPanel>
</UserControl>
